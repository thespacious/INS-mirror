controllers.controller('homeQuotesCtrl', function ($scope, $state, $stateParams) {
    //hit the server first with agent creds at end of that exchange get object with seesion id and quotes 
    //we will hit the main template endpoint
});